app.service('Amazon', ['$resource', '$log', function($resource, $log){

    // updated ttype modal
  var Amazon = this;

  // DELETE OBJECT

  Amazon.deleteObject = function(objectname) {
    // var AWS = require('aws-sdk');
    // AWS.config.loadFromPath('./credentials-ehl.json');
    var s3 = new AWS.S3();
    var params = {  Bucket: "grapplegame-user-videos-dev", Key: objectname };
    s3.deleteObject(params, function(err, data) {
      if (err) console.log(err, err.stack);  // error
      else     console.log();                 // deleted
    });

  $log.log(params)
  };


}])
