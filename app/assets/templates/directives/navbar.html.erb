<!-- NAVBAR START -->
<nav  ng-class="toggleClass" class="navbar navbar-fixed-top" role="navigation">
  <!-- Top Menu Items -->
  <ul class="homenav navbar navbar-default top-nav">
    <!-- Navbar toggle button -->
    <div ng-class="hideText">
      <div ng-click="arrow = !arrow; navToggle()" ng-class="{'hamburger': !arrow, 'arrow-right': arrow}" id="" class="nav-toggle">
        <p class="toggle-p"ng-show="!arrow"></p>
        <p class="toggle-p" ng-show="arrow"></p>
        <span class="togglespan" ></span>
      </div>
    </div>

     <span class="ttype-nav-text" ng-class="hideText">{{currentPositionName}}</span>


     <span ng-click="backToTechs()" ng-class="techniqueBack" class="ttype-nav-text small-text hideTextActive"><i class="fa fa-arrow-circle-left back-arrow"></i><span class="" > {{currentPositionName}}</span></span>
     

    <!-- sidemenu -->
    <li id="aside-icon" class="pull-right no-touch">
      <span ng-click="openAside('right', false)" href="" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-user"></i></span>
    </li>

  </ul>
</nav>
<!-- NAVBAR END -->
