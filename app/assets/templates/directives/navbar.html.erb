<!-- NAVBAR START -->
<nav  ng-class="toggleClass" class="navbar navbar-fixed-top" role="navigation">
  <!-- Top Menu Items -->
  <ul class="homenav nav navbar navbar-default top-nav">
    <!-- Navbar toggle button -->
    <div ng-click="arrow = !arrow; navToggle()" ng-class="{'hamburger': !arrow, 'arrow-right': arrow}" id="test" class="nav-toggle">
      <p class="toggle-p"ng-show="!arrow"></p>
      <p class="toggle-p" ng-show="arrow"></p>
      <span class="togglespan" ></span>
    </div>

    <!-- sidemenu -->
    <li class="pull-right">
      <a ng-click="openAside('right', false)" href="" class="dropdown-toggle" data-toggle="dropdown"><i class="fa fa-user"></i></a>
    </li>
  </ul>
</nav>
<!-- NAVBAR END -->
